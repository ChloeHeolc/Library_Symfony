{% extends "block.html.twig" %}

{% block main %}

	<h1>{{ genre.name }}</h1>
	<p>{{ genre.description }}</p>
	{% for book in genre.books %}

		<h4>
		<a href="{{ path('book_show', {'id' : book.id} ) }}">
		{{ book.title }}</a>
		</h4>

	{% endfor %}

{% endblock %}