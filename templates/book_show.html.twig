{% extends "block.html.twig" %}

{% block main %}

    <h1>{{ book.title }}</h1>
    <p>{{ book.resume }}</p>


    {% if book.year %}
    <h3>Année de parution :
        {{ book.year }}</h3>
    {% endif %}
    {% if book.writer.name %}
    <h3>Auteur :
        <a href="{{ path('writer_show', {'id' : book.id} ) }}"> {{ book.writer.name }}</a>
    </h3>
    {% endif %}
    {% if book.genre %}
    <h4>Genre :
        {{ book.genre.name }}</h4>
    {% endif %}

    {#
    <h2>
        <a href="{{ path('book_show', {'id' : book.id} ) }}">
            {{ book.title }}</a>
    </h2>
    #}

{% endblock %}