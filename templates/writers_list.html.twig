{% extends "block.html.twig" %}

{% block main %}



    {% for writer in writers %}
		<h2>
			<a href="{{ path('writer_show', {'id' : writer.id} ) }}">
				{{ writer.name }}
			</a>
		</h2>
		<p>{{ writer.bio }}</p>
		<hr>
	{% endfor %}
            
{% endblock %}